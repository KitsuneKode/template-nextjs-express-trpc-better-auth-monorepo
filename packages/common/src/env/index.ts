/**
 * Environment configuration exports
 * Provides type-safe env access across the monorepo
 */

export { clientEnv, type ClientEnv } from './client'
export { sharedEnvSchema, type SharedEnv } from './shared'
