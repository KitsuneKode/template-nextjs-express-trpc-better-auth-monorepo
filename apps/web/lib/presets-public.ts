/**
 * Public preset display data for the marketing site.
 * Sourced from @arche-template/registry (single source of truth with the CLI).
 */

export {
  formatSupportStatus,
  SUPPORT_LABELS,
  type SupportStatus as PublicSupportStatus,
} from '@arche-template/registry'

export {
  PUBLIC_PRESET_ENTRY_ROWS,
  PUBLIC_PRESET_ROWS,
  devScaffoldCommand,
  type PublicPresetId,
  type PublicPresetRow,
} from '@arche-template/registry/display'
