# Web Workspace

This workspace hosts the Next.js frontend.

- Start with `AGENTS.md` in this folder for the fast path.
- Use `app/layout.tsx`, `app/page.tsx`, and `trpc/` for the real runtime entrypoints.
- Read `../../docs/start-fresh.md` before removing the template showcase surfaces.
