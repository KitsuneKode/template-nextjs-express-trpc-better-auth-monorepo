{
  "name": "@acme/polyglot-worker",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "lint": "oxlint",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1",
    "typescript": "^5"
  }
}
