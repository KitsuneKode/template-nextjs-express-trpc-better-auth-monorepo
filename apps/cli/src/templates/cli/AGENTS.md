# CLI

## Purpose

Command-line tool for this project.

## Read First

- `src/index.ts` — entrypoint, arg parsing, command routing
- `package.json` — scripts, bin entry, dependencies

## Owns

- CLI commands and argument parsing
- stdout/stderr output and formatting
- User-facing prompts and progress indicators
