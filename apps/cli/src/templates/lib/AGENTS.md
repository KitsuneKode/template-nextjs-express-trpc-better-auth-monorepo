# Library

## Purpose

Shared TypeScript library package for this project.

## Read First

- `src/index.ts` — public API entrypoint and barrel exports
- `package.json` — scripts, main/types entry, dependencies

## Owns

- Shared utilities, types, and constants
- Pure-logic modules with no runtime side-effects
