# Tests Workspace Notes

## Purpose

`tests` is the repo test workspace. It now covers the tooling scripts and can
grow into broader app coverage over time.

## Read First

- `package.json`
- `src/index.ts`

## Current State

- Repo tooling tests live under `src/toolings`.
- This workspace does not yet cover the full web/server/runtime stack.

## Common Tasks

- add more Bun tests under `src/`
- expand from tooling checks into runtime and integration coverage
- keep the test layout aligned with real coverage, not placeholder folders

## Update When

Update this file when real test suites, helpers, or coverage expectations are
added.
